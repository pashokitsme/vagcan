[package]
name = "vag-hex"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[dependencies]
vag-transport = { path = "../vag-transport" }
thiserror.workspace = true

# Real cable backends are wired once the USB capture defines the protocol:
#   ftdi / libftd2xx-rs for the D2XX path, or serialport for the VCP path.
# Kept out of the tree until frame.rs / init.rs are pinned from the trace.
