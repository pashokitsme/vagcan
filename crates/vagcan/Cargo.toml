[package]
name = "vagcan"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[dependencies]
vag-hex = { path = "../vag-hex" }
tokio = { workspace = true, features = ["rt-multi-thread", "macros"] }
clap = { version = "4", features = ["derive"] }
anyhow = "1"
