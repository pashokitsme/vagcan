[package]
name = "vag-uds-client"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[dependencies]
# Reading the per-car unit-number table; see address.rs — host only.
serde_json = { workspace = true, optional = true }
vag-uds-transport = { path = "../vag-uds-transport", default-features = false }
# `read.rs` is the ONLY reason this crate touches vag-data-labels, and `read.rs` is
# host-only: the board executes a plan with the scaling already baked in and
# could not hold an 88 MB catalog in 400 KB of SRAM if it wanted to.
vag-data-labels = { path = "../../data/vag-data-labels", optional = true }
thiserror.workspace = true

[dev-dependencies]
vag-uds-capture = { path = "../vag-uds-capture" }
vag-uds-transport = { path = "../vag-uds-transport", features = ["serde", "test-util"] }
tokio = { workspace = true, features = ["rt", "macros"] }

[features]
default = ["std"]
# The host build. With `--no-default-features` the crate is `no_std` + `alloc`
# and loses exactly two modules:
#
#   address.rs  reads the filesystem (the per-car unit-number override table),
#   read.rs     decodes a measurement against a `vag-data-labels` catalog,
#
# which is also what drops the `vag-data-labels` edge. Everything the board needs —
# the UDS client, the PDU allowlist, DTC parsing, the gateway installation
# list, identity reads, software ISO-TP — stays.
std = ["dep:serde_json", "dep:vag-data-labels", "thiserror/std", "vag-uds-transport/std"]
