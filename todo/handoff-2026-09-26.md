# Hand-off to a cloud session — 2026-09-26

Written by the local session before it moved to the cloud. **Read `CLAUDE.md`, then this file,
then `todo/README.md`.** Delete this file in the final docs PR (task 4): it is a hand-off, not
a record. Answer the owner in Russian; refer to yourself as «она».

## 1. What the cloud cannot do, and who does it instead

The cloud has the repository and GitHub. It does **not** have:

| missing | consequence | who does it |
|---|---|---|
| the car, the board, BLE, USB serial | no `vagcan` against a car, no `dashsim`, no flashing | the owner, locally |
| `~/.vagcan/` (label cache, the owner's `dash.toml`, `plan.json`, proven measurement rows) | tests behind `need_rows!` **skip** (29 in `plan`, `watch`, `measure`); a firmware image with a real plan cannot be built | the owner, locally |
| `/Users/pavel.smirnov/Downloads/SK37X` (the ODIS project) | no `vagcan setup` | the owner, locally |
| `~/CAD/projects/vagcan/` (the enclosure) | the CAD itself cannot be edited; only `todo/dash/15` notes | the owner / a local session |

**Never invent data to make up for the missing files** (`CLAUDE.md`: no car-specific data in
code; the owner's working style: no fake data ever). A test fixture built from protocol facts
is fine; a copy of the owner's plan or cache in the repository is not.

## 2. Tools the cloud environment needs

| tool | why | how |
|---|---|---|
| Rust **stable** ≥ 1.85, edition 2024 | everything | `rustup toolchain install stable` |
| components `rustfmt`, `clippy`, `rust-src`, `llvm-tools` | fmt/clippy gates; `rust-src` for the firmware's `build-std`; `llvm-size`/`llvm-nm` for `ram-budget.sh` | `rustup component add rustfmt clippy rust-src llvm-tools` |
| target `riscv32imc-unknown-none-elf` | firmware and the `no_std` checks | `rustup target add riscv32imc-unknown-none-elf` (the firmware's `rust-toolchain.toml` also asks for it) |
| `libdbus-1-dev libudev-dev pkg-config` | `btleplug` and `serialport` link against them on Linux | `sudo apt-get install -y libdbus-1-dev libudev-dev pkg-config` |
| `jq` | the `.claude/settings.json` rustfmt hook no-ops without it | `apt-get install -y jq` |
| `gh`, authenticated to `pashokitsme/vagcan` | PRs, CI status | the cloud's GitHub access |
| **not needed** | `espflash`, Bluetooth, serial ports | — |

Environment for every firmware command: `VAGCAN_DASH_NO_CAR=1` (an empty plan; the build
says "do not flash this image" — correct, nothing built in the cloud is flashed).

Disk: a workspace `target/` is ~12 GB, the firmware's 3–6 GB. On 2026-09-26 the local disk
filled twice and killed an agent. One `target/` per checkout; delete a finished worktree's
`target/`; never give a subagent a fresh `CARGO_TARGET_DIR`.

## 3. The gates (all green before any push)

```
cargo fmt --all -- --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
RUSTFLAGS="--force-warn dead_code" cargo check --workspace      # never --all-targets
```

In `crates/dash/vag-dash-fw` (not a workspace member):

```
cargo fmt -- --check
VAGCAN_DASH_NO_CAR=1 cargo clippy --release --bins -- -D warnings
VAGCAN_DASH_NO_CAR=1 cargo clippy --release --bins --no-default-features -- -D warnings
VAGCAN_DASH_NO_CAR=1 ./ram-budget.sh
```

In `research/dash/host` when touched: `cargo fmt -- --check`, `cargo clippy --all-targets -- -D warnings`, `cargo test`.
`no_std`: `cargo check -p vag-uds-client --no-default-features --target riscv32imc-unknown-none-elf` (and `-transport`, `-can`).

**RAM (owner, 2026-09-26, `CLAUDE.md` "Firmware RAM"):** every firmware change reports
`ram-budget.sh` before and after for both builds, calls out static growth over 1 KB, and keeps
buffers packed (a pixel is a bit; running sums, not sample arrays). Baseline on `master`
4486567: ble static 138,864 / stack 157,100; no-ble 129,404 / 188,448. The script's ceilings
and floors: ble 156,000 / 140,000; no-ble 145,000 / 168,000.

## 4. How work is done here (the owner's rules)

- **Every merge is a PR the owner merges.** Never merge, never push `master`, never enable
  auto-merge. Push only your own work branch.
- **Review before a PR:** reviewer subagents with separate lenses (correctness/failure paths,
  safety, RAM, docs vs code); fix; the same reviewers confirm; repeat until no objections. The
  owner said: flash only after everything is reviewed by subagents and merged via PR.
- **Commits:** Conventional Commits; end with `Claude-Session: <this cloud session's URL>` and
  `Assisted-By: Claude <model> <noreply@anthropic.com>` — **never `Co-Authored-By`**.
- Stage by explicit path. Never `git add -A`/`.`/`commit -a`, `reset --hard`, `stash`,
  `clean`, `checkout --`.
- UDS allowlist `0x22 0x19 0x10 0x3E`, never a write service. No car data in code.
- `cargo fmt --all` from the root **and** `cargo fmt` in each non-member crate before a push.
- GitHub issues/projects: **not** now (owner: «пока забей на гх»); the `gh` token is the
  owner's, and he objected to content authored in his name.
- `README.md` roadmap updated in the same commit that finishes, adds or drops an item.

## 5. State at hand-off

| what | where | state |
|---|---|---|
| PR #10 `fix/sanity-2026-09-26` | GitHub | fresh-eyes fixes (replay hex/field range, 11-bit response ids, one address rule, blink after adapter); reviewed clean; CI was running. **Owner merges.** |
| PR #11 `chore/cleanup-2026-09-26` | GitHub | docs cleanup, status vs code, next list, cleanup-skill rules; LIMIT recorded as established. **Owner merges.** |
| `feat/stalk-stopwatch` (dash/19) | pushed `30377d6` | phase 1 reviewed; phase 2 committed, gates green, one internal review; independent review owed (§6 task 1) |
| `feat/fault-count` (dash/20) | pushed `03ae320` | phase 1 reviewed; based on e899abc, **before** #7–#9 |
| this branch `chore/handoff-2026-09-26` | pushed | only this file |

## 6. Tasks, in order

### Task 1 — dash/19 phase 2: independent review, then PR

Branch `feat/stalk-stopwatch`, pushed at `30377d6`. Spec `todo/dash/19-stalk-and-stopwatch.md`
on the branch, rewritten as built. Commits: `b462b2f` (merge of master 4486567), `c96758d` (the
feature: plan, render, firmware, settings, replay), `30377d6` (docs: dash/19, dash/14, README).

What the local agent reported (verify against the code; one reviewer subagent already ran
inside that agent — an **independent** review loop is still owed):
- `[stalk]`/`[stopwatch]` in `dash.toml` → plan (`vag-cli-core/src/dash.rs`) → `plan.rs`. State
  texts matched exactly and in full, carried as intervals; a test checks the board's lookup
  against the laptop's `level_for` value by value.
- Build refusals, each tested and listed in dash/19: unknown field/state text; a quantity
  where states are expected; one state for two buttons; a cruise field that is not states;
  **a cache from before PR #9's level bounds**; speed not a `[[channel]]`, with an offset, or
  factor ≤ 0; a mark 0, repeated or > 3; a negative factor.
- Rates, fixed in code in `vag-dash-render/src/plan.rs` (`Plan::rates_in`), not from `hz`:

  | mode | rocker (`70C:1105`) | cruise status (`01:203C`) |
  |---|---|---|
  | gate closed | 2 Hz | 5 Hz |
  | gate open (switch OFF and cruise status off) | 20 Hz | 5 Hz |
  | stopwatch up | 10 Hz | 5 Hz |

  The switch is decoded from the rocker's own answer. Cruise status counts only if < 600 ms
  old. While armed/running every page cell drops to background **except alarm-watched ones**.
  Debounce: two consecutive reads agree, gate open on both. A press registers for sure at
  ≥ 0.1 s (20 Hz) / ≥ 0.2 s (10 Hz); the capture has single-read taps, so hold to register.
- **LIMIT = "neutral ohne Limiterverbau" is established** (the owner pressed it; the state
  appears only on those presses, never at rest) — dash/19 says so; no `4383` witness step.
- Firmware: the lever fed once per new answer; the stopwatch in a `static`, stamped with each
  speed answer's arrival, reset whenever the mode turns on or off; the stopwatch stays on
  screen after an alarm hands back; a finished run saved to flash at once if nothing else is
  unsaved. Settings v1 → v2 (`last_run`) **migrated, not discarded**, with a host test on a
  hand-written v1 image (in `research/dash/host/tests/`).
- `MAX_MARKS` 3; page words EN/RU, units stay Latin (the unit font has no Cyrillic).
- `dev recording dash` says it replays neither the lever nor the stopwatch.
- Gates green: 1812 workspace tests, host 40, fmt, clippy, firmware clippy both builds,
  dead-code clean. A generated `plan.rs` with a lever and a stopwatch compiled under
  `-D warnings` in a scratch crate — **not in CI**; consider a CI-run test for that path, from
  a hand-written protocol-shaped fixture (never the owner's data).
- RAM (empty plan): ble static 138,864 → 139,224 (+360), stack 157,100 → 156,740; no-ble
  129,404 → 129,756 (+352), stack 188,448 → 188,096. Stopwatch 304 B, settings +28 B.
- `todo/README.md` and `todo/dash/README.md` untouched (PR #11 owns them); `README.md`
  roadmap has 2 changed lines — may conflict with #11, keep #11's wording plus dash/19's fact.

Open questions for the owner (ask; give the recommendation):
1. **Saving a run on the road** erases and writes a flash sector mid-drive, from the panel
   task, while the bus is polled and BLE may be up; until now flash was written only on an
   explicit `save`. Does the write drop CAN frames or BLE events? Needs a bench check by the
   owner. Recommendation: keep the run in RAM and write it when the car is stopped (speed 0)
   or on the next explicit save, unless the bench shows no drop.
2. The board carries old v1 settings forward but writes v2 only on its next save — fine, just
   tell the owner.

Then: independent review with separate lenses (rates and the gate's safety; the stopwatch vs
`vag-cli-measure::derive::start`/`Track::crossing`; RAM and the flash write/migration; plan
build refusals; docs vs code), fix, same reviewers confirm, merge `origin/master` if #10/#11
landed, re-run gates, PR for the owner. The owner's `dash.toml` snippet is in dash/19 (needs
`02:380B` as a `[[channel]]`, hz 50, and `km_h_per_unit = 0.0` until measured) — the owner
applies it locally.

### Task 2 — dash/20 phase 2: the fault count in the firmware

Branch `feat/fault-count`. Spec `todo/dash/20-fault-count.md` on it.

First, bring it up to date:
- merge `origin/master` (after #10 lands: `UnitAddress::from_request` returns `None` past
  `0x795`; `walk_order`/`faultcount` must count those as their own reason, not as "past
  `7BF`" — `faultcount.rs` `Tally::outside_block` wording, found in review);
- **the merged tree does not compile its tests**: `vag-dash-render` `screen.rs` test
  `the_plain_half_of_a_blink_draws_exactly_the_page_with_nothing_inverted` builds `Board`
  without the new `faults` field (E0063) — add `faults: None`;
- `todo/README.md` conflicts — take master's.

**Ask the owner before writing phase 2** (open design questions from the review; give the
recommendation, wait for his answer):
1. The board decodes the gateway's list at run time and addresses units no plan holds. That
   breaks `todo/dash/README.md`'s rules "the board resolves nothing", "a plan cannot sweep",
   "a unit is polled only when its part number matches". Options: (a) keep it and rewrite
   those rules to allow exactly this read-only walk; (b) put the unit list in the plan at build
   time (the survey jsonl the plan already reads lists them), which also removes the bitmap
   decoder and `MAX_UNITS` guards from the board. Recommendation: (b), unless the owner wants
   the count to see a unit fitted after the plan was built.
2. `MAX_UNITS = 40` comes from this car alone; the BLE guard allows 64; large MLB cars may list
   more. Recommendation: 64, the guard's number.
3. A unit answering `7F 19 78` holds the one link up to `PENDING_DEADLINE` (10 s, `dash.rs`),
   freezing the panel and the alarms. Recommendation: the count uses its own short deadline
   (≈1.5 s, then "no answer") and does not start or continue while the stopwatch is up.
4. The badge is counted once and shows no age; hidden at 0 looks like "not counted". The
   owner chose once-at-start and hidden-at-0; ask only whether "not counted" (no list, too
   many units, still waiting) should look different from 0.
Also: the badge (bottom-right, (245,55)) and the stopwatch page both use the bottom-right —
decide who wins while the stopwatch is up. The doc's "guarded like `survey`" → "capped"
(`survey` is guarded by `require_stationary`; this count only by a unit cap). Reading `0x19`
while moving is within `CLAUDE.md` (no `10`, nothing changes in a unit) — confirmed by review.

Then phase 2: wire the count into the firmware's one bus owner (no second link owner), start
at ≥ 10 s after boot and after at least one plan unit answered, log per-unit outcomes on USB,
draw the badge. RAM before/after. Review loop, PR.

### Task 3 — whatever the owner merges in between

After each merge the owner does: `git fetch`, merge `origin/master` into the open branches,
re-run gates. Watch CI on open PRs with `gh pr checks`; fix red.

### Task 4 — final docs PR (after dash/19 and dash/20 merge)

- `README.md` roadmap (dated first line) and `todo/README.md`: a new dated status; move the
  2026-09-26 status verbatim to `.archive/tasks/roadmap-history.md`.
- Retire finished task files to `.archive/tasks/done/dash/` **only** if nothing in them is
  left but car checks recorded elsewhere; otherwise keep them live. Candidates: dash/04,
  dash/19, dash/20 — each still has car checks, so most likely they stay, with their state
  lines updated.
- `todo/dash/15-enclosure.md` — the CAD lessons from 2026-09-26 (CAD lives in
  `~/CAD/projects/vagcan/housing.py`, not here):
  - first print: only the ESP latched; the barbs sat 0.04 mm inside the board edge; fixed with
    `REACH` 1.4 mm and `HOLD_PLAY` 0.2 mm, beams 12 mm; edge ledges instead of rails; supports
    under the chips removed;
  - the ESP is 2 mm thick at its latch (antenna), the other boards bare PCB there;
  - the face was flimsy with slits on four sides → solid 1.6 mm walls, display latches as free
    beams in 3 mm end bays («язычки внутри, без прорезей»);
  - the buck board is 25 × 17 mm; the CAN board's pins stick out 20 mm and may overlap the
    buck (nothing under them there); at least 7 mm between the CAN board and the buck
    (owner: «главное хоть 0.7см оставь»); built: CAN→buck 14.1 mm, buck→ESP 5.0 mm;
  - the housing length is fixed by the display: 109.7 × 36.9 × 28.2 mm, acrylic 109.7 × 36.9;
  - test coupons for printing: `housing-flat-clips-coupon` (~27 min), `housing-flat-latch-coupon` (~12 min);
  - still a guess: the pins' height above the CAN board (2.5 mm).
- Delete this hand-off file.
- Run the cleanup skill (`.claude/skills/cleanup/SKILL.md`) phases 1 and 5 before the PR.

### Task 5 — what only the owner can do (list it for him when asked; do not attempt)

1. `vagcan setup /Users/pavel.smirnov/Downloads/SK37X` — the cache predates PR #9; until then
   `watch` prints the lever as hex and dash/19's plan build refuses.
2. Add the reviewed `[stalk]`, `[stopwatch]` and `02:380B` channel to
   `~/.vagcan/dash/XW8AD4NE9JH008917/dash.toml`, `vagcan dev dash build XW8AD4NE9JH008917`.
3. Build the image from `master` in `crates/dash/vag-dash-fw` (without `VAGCAN_DASH_NO_CAR`) and
   flash — only after everything is merged; flashing in stages (master's −6.0/−4.5 + blink
   first, then dash/19, then dash/20) keeps a fault on the car attributable. The owner decides.
4. Print the CAD coupons, report the fit.
5. Car checks: dash/17 §4 rest (cable through the board with USB before OBD power, the
   moving-car guard via `bleuds`, CANable on car traffic, ESC channels, §2 item 8); alarms on
   a drive at −6.0; dash/18 boost on a pull; dash/19 paging with cruise off, the `380B` factor
   on a steady stretch, a 0–100 run beside `vagcan measure --ble`; dash/20 count vs
   `vagcan faults`; faults ODIS-only; dash/06; reverse-gear code; sweep constants; watch and
   measure on all 15 units.
6. OLED: waits for the display.
