# Vendored external tool — cloned locally, not committed. Reinstall with:
#   git clone --depth 1 https://github.com/bri3d/VW_Flash scripts/vendor/VW_Flash
#   cd scripts/vendor/VW_Flash && uv sync
# (lzss build is only needed for flashing, which frfscope does not do.)
VW_Flash/
